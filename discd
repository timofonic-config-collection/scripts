#!/bin/sh
# Script by Ypnose - http://ypnose.org
# HDD daemon

# TODO: REWORK THIS CRAPPY SCRIPT!!!

DATE=$(date '+%A %d %B')
TIME=$(date '+%R')
# Not supported by Desktop
#APM=$(/usr/sbin/hdparm -I /dev/sda | grep 'Advanced power')
PCC=$(/usr/sbin/smartctl -A /dev/sda | grep -i 'Load_Cycle_Count')

printf "%s\n" "--> $DATE | $TIME <--"
printf "%s\n\n" "$PCC"
exit 0
